/**
 * Lucy Class — gam màu admin / CTA (đồng bộ sidebar AdminLayout, Dashboard, Chấm công…)
 * Tailwind `primary-*` trong theme mặc định là blue; các trang admin dùng bộ hex này.
 */
export const LUCY_BRAND = {
  primary: '#1C695C',
  primaryLight: '#3FA48F',
  primaryDeep: '#1C6970',
  /** Nền nhạt cho header bảng / card (mint, cùng hệ với primary) */
  surfaceMint: '#E8F5F3'
};
